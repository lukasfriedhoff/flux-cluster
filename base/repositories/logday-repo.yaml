apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: logday
  namespace: flux-system
spec:
  interval: ${flux_gitrepo_interval:=10m0s}
  ref:
    branch: develop
  secretRef:
    name: ${logday_repo_secret_name:=github-read-logday-prod}
  url: https://github.com/lukasfriedhoff/logDay.git
