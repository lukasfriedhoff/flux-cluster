apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: lldap-app
  namespace: flux-system
spec:
  dependsOn:
    - name: namespaces
    - name: secrets
    - name: cnpg-app
    - name: longhorn-app
    - name: traefik-app
  interval: 1h0m0s
  path: ./apps/lldap
  prune: true
  retryInterval: 10m0s
  sourceRef:
    kind: GitRepository
    name: flux-apps
  suspend: ${lldap_suspend:=true}
  wait: true
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: base-config
